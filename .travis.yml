---
language: elixir
elixir:
  - "1.7.4"
otp_release:
  - "21.1.4"

addons:
  postgresql: "10.1"

services:
  - postgresql

sudo: false
dist: trusty

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  - mix do ecto.create, ecto.migrate

script:
  - mix test
